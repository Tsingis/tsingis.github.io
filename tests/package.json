{
  "name": "tsingis.github.io",
  "scripts": {
    "playwright": "npx playwright test",
    "playwright:update": "npx playwright test --update-snapshots",
    "playwright:setup": "npx playwright install --with-deps",
    "server": "http-server ../src --cors --host 127.0.0.1 -p 3333"
  },
  "devDependencies": {
    "@axe-core/playwright": "^4.12.1",
    "@playwright/test": "^1.61.0",
    "@types/node": "^26.0.0",
    "http-server": "^14.1.1",
    "typescript": "^6.0.3"
  }
}
